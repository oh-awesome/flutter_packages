name: camera_ohos_example
description: Demonstrates how to use the camera plugin.
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  camera_ohos:
    # When depending on this package from a real application you should use:
    #   camera_android: ^x.y.z
    # See https://dart.dev/tools/pub/dependencies#version-constraints
    # The example app is bundled with the plugin so we use a path dependency on
    # the parent directory to use the current plugin's version.
    path: ../
  camera_platform_interface: ^2.6.0
  flutter:
    sdk: flutter
  quiver: ^3.0.0
  path_provider:
    git:
      url: "https://gitcode.com/CPF-Flutter/flutter_packages.git"
      path: "packages/path_provider/path_provider"
      ref: "provider-v2.1.5-ohos-1.0.1"
  video_player:
    git:
      url: "https://gitcode.com/CPF-Flutter/flutter_packages.git"
      path: "packages/video_player/video_player"
      ref: "video_player-v2.10.1-ohos-1.0.1"

dependency_overrides:
  path_provider_foundation:
    git:
      url: "https://gitcode.com/CPF-Flutter/flutter_packages.git"
      path: "packages/path_provider/path_provider_foundation"
      ref: "provider-v2.1.5-ohos-1.0.1"
  pigeon:
    git:
      url: "https://gitcode.com/CPF-Flutter/flutter_packages.git"
      path: "packages/pigeon"
      ref: "pigeon-v26.3.4-ohos-1.0.0"


dev_dependencies:
  build_runner: ^2.1.10
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
